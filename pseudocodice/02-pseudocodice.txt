Pseudocodice

if (esecuzioneDiDebug) {
	String stringPath = <assegno un path di default>
}
else {
	Leggo una stringa inserita dall'utente alla console e la memorizzo in stringPath;
}
eseguire il cd sulla cartella specificata
eliminare tutti quei file con formato diverso da .ccs
Prendiamo la lista di tutti i file nella cartella e memorizziamo i loro nomi nella lista "fileList" -> {
	eseguiamo ls -l (windows: dir)
	memorizziamo l'output in una lista 
	scorriamo la lista e su ciascun elemento chiamiamo un metodo che ci parsa l'elemento e ci restituisce il nome del file
	aggiungiamo il nome del file ad un'altra lista contentente i nomi dei file.
}

Parsing di "ls -l" -> {
	Contiamo la presenza di 8 gruppi di spazi consecutivi. Al termine dell'ottavo inizia il nome del file.
	Casi particolari:
	- Se c'è un link, questo è seguito dal simbolo "->" e dal percorso di destinazione.
	- Se ci sono spazi, il nome file si trova all'interno di apici singoli (').
}

elaboraSingoloFileOriginale(String file) {
	cerca la riga che inizia per "proc ALL";
	in quella riga prendi i nomi dei metodi e aggiungili alla LinkedList "metodiList";
	sul terminale "terminaleMetodiOriginali" fai la load in cwb di quel file;
	avvia il comando per calcolare la size dei metodi che hai estratto da "proc ALL";
	parsa l'output del comando estraendo la size;
	memorizza le size in un oggetto singleton "sizeMetodiOriginali" che estende la classe "sizeMetodi".
}

elaboraFileOriginali(LinkedList<String> fileList) {
	//per ciascun .ccs calcola la size dei metodi
	cicla sulla filelist {
		elaboraSingoloMetodoOriginale(fileList.remove());
	}
}